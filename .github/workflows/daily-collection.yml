name: Daily CASCO collection

on:
  push:
    branches:
      - main
    paths:
      - "collector/**"
      - "core/**"
      - "database/**"
      - "scripts/run_collection.py"
      - ".github/workflows/daily-collection.yml"
      - "requirements.txt"
  schedule:
    # 03:00 UTC every day (06:00 Moscow time).
    - cron: "0 3 * * *"
  workflow_dispatch:
    inputs:
      insurer:
        description: "Optional insurer slug; leave empty to collect all 11"
        required: false
        type: string

concurrency:
  group: daily-casco-collection
  cancel-in-progress: true

jobs:
  collect:
    name: Collect ${{ matrix.insurer }}
    runs-on: ubuntu-latest
    timeout-minutes: 12
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        insurer:
          - reso
          - vsk
          - ingos
          - renins
          - alfa
          - soglasie
          - rgs
          - t-insurance
          - sber
          - yugoria
          - sovcom

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      GROQ_MODEL: openai/gpt-oss-20b
      COLLECTION_TRIGGER: ${{ github.event_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt

      - name: Compile collector
        run: python -m py_compile collector/*.py core/*.py database/repositories/*.py

      - name: Smoke test extraction quality
        run: python -m unittest tests.test_deterministic tests.test_evidence_quality tests.test_condition_audit

      - name: Run collection
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.insurer == '' || inputs.insurer == matrix.insurer }}
        run: python scripts/run_collection.py --insurer "${{ matrix.insurer }}"
