[project]
name = "insurance-comparator"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
  "flask",
  "requests",
  "beautifulsoup4",
  "PyPDF2",
  "pypdf",
]

[tool.vercel]
entrypoint = "vercel_entry:app"
