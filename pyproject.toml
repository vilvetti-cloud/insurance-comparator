[project]
name = "insurance-comparator"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
  "flask",
  "requests",
  "beautifulsoup4",
  "PyPDF2",
  "pypdf",
  "PyMuPDF",
  "psycopg[binary]>=3.2,<4",
]

[tool.vercel]
entrypoint = "vercel_db_entry:app"
